Use Github CLI

Take branchname from user, if branch name is not provided use a meaning full name.

Once branch is created, stage all changes, add newly created file.

Commit with sensible message, push to newly create branch.

Create a pr against `main` branch and give me the pR Link.